[phases.setup]
nixPkgs = ['nodejs', 'python311', 'python311Packages.pip', 'gcc']

[phases.install]
cmds = ['npm install', 'python3.11 -m pip install -r grok-voice-agent/requirements.txt']

[start]
cmd = 'bash start.sh'